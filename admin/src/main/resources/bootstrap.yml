server:
  port: ${ADMIN_PORT:8022}

spring:
  application:
    name: ${ADMIN_APPLICATION_NAME:discovery}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL_LIST:http://localhost:8761/eureka}
